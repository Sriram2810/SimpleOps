[project]
name = "project"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "jupyter>=1.1.1",
    "keras>=3.9.2",
    "mlflow>=2.21.3",
]

[dependency-groups]
dev = [
    "ipykernel>=6.29.5",
]
